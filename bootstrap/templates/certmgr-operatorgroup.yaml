apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: cert-manager-operator-group
  namespace: {{ .Values.operators.certManager.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.operators.certManager.namespace }}
