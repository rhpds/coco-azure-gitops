apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.operators.sandboxedContainers.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.operators.sandboxedContainers.syncWave }}"
