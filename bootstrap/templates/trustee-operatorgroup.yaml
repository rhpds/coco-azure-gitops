apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: trustee-operator-group
  namespace: {{ .Values.operators.trustee.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.operators.trustee.namespace }}
